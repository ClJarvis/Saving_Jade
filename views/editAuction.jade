extends layout
link(rel='stylesheet', href='stylesheets/auction.css')

block content

	div.container.bg-primary

		div.row.success
		div.pull-left.col-md-6.col-md-offset-2
			div.enterItems.success
				div.items_form(method="POST", action="/user/register", name="itemsForm")
				h3 Edit Your Auction Items!
				p Title
					input(type="text" id="title" name="title")
				P Price
					input(type="text" id="price" name="price")
				p Seller
					input(type="text" id="seller" name="seller")
				P Auction Ends
					input(type="text" id="endDate" name="endDate")

- for {var i = 0; i < items.length; i++ }
{ var currentItem = items[i]; }
<{r id="shop_<%= currentItem._id %>"}
ul
							li.h4 #{item.title}
							li.h5 #{item.price}
							li.h5.sell #{item.seller}
							li.h5.date #{item.endDate}
							input(type="button" class="editButton", value="edit" id="#{item._id}")
							input(type="button" class="deleteButton" value="delete" 	id="#{item._id}")s
				<td><%= currentItem.title %></td>
				<td><%= currentItem.price %></td>
				<td><%= currentItem.seller %></td>
				<td><%= currentItem.endDate %></td>
				input( type="submit", class="editButton" id="<%= currentItem._id %>">Edit
				input( type="submit", class="deleteButton", id="<%= currentItem._id %>">Delete</a></td>
